## revdepcheck results

We checked 3 packages that depend on erplots (via `Imports`/`Suggests`),
comparing R CMD check results across CRAN and dev versions of this
package. Of these, only `erglm` is presently on CRAN (as a
reverse-`Suggests`, accepted 2026-08-08); `emaxnls` and `ertte` are
GitHub-only companion/downstream packages, checked here as a courtesy
rather than because CRAN requires it.

 * We saw 0 new problems
 * We failed to check 0 packages

