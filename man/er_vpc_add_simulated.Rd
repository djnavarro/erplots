% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-vpc-add.R
\name{er_vpc_add_simulated}
\alias{er_vpc_add_simulated}
\title{Add the simulated-data layer to an \code{er_vpc} VPC}
\usage{
er_vpc_add_simulated(
  object,
  model = NULL,
  sim = NULL,
  nsim = 100,
  seed = NULL,
  conf_level = 0.95,
  probs = c(0.1, 0.5, 0.9),
  style = er_style_vpc_simulated_errorbar,
  ...
)
}
\arguments{
\item{object}{Partially constructed VPC (has S3 class \code{er_vpc}), which
must already have an observed layer (see \code{\link[=er_vpc_add_observed]{er_vpc_add_observed()}}).}

\item{model}{A fitted model implementing \code{\link[=er_simulate]{er_simulate()}} with
\code{sim_resp}. Mutually exclusive with \code{sim}.}

\item{sim}{Simulated data with matching exposure/response/\code{group_by}
columns and \code{sim_id}. Mutually exclusive with \code{model}.}

\item{nsim}{Number of simulation replicates, only used with \code{model}.}

\item{seed}{Optional RNG seed, only used with \code{model}.}

\item{conf_level}{Confidence level for the simulated-side interval.}

\item{probs}{Percentiles to compute for the continuous-x ribbon
builder; should match whatever \code{probs} was passed to
\code{\link[=er_vpc_add_observed]{er_vpc_add_observed()}} (ignored by the default errorbar builder).}

\item{style}{A function determining how the simulated layer is drawn;
see \code{\link[=er_style_vpc_simulated]{er_style_vpc_simulated()}}.}

\item{...}{Additional named arguments forwarded to \code{style}.}
}
\value{
\code{object}, with \code{object$layer$simulated} populated.
}
\description{
Bins simulated data using the same cutpoints \code{\link[=er_vpc_add_observed]{er_vpc_add_observed()}}
already computed, and summarizes it (mean + a percentile interval
across replicates, plus simulated percentile bands for a
continuous/count response).
}
\details{
\code{sim} and \code{model} are mutually exclusive; supply exactly one. \code{model}
is preferred when it implements \code{\link[=er_simulate]{er_simulate()}} with \code{sim_resp},
since a VPC needs response-level simulated observations rather than
only mean predictions -- this function errors informatively if
\code{sim_resp} isn't available.
}
\seealso{
\code{\link[=er_vpc]{er_vpc()}}, \code{\link[=er_vpc_add_observed]{er_vpc_add_observed()}}, \code{\link[=er_style_vpc_simulated]{er_style_vpc_simulated()}}
}
