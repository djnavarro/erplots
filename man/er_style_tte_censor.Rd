% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-tte-style-censor.R
\name{er_style_tte_censor}
\alias{er_style_tte_censor}
\alias{er_style_tte_censor_ticks}
\title{Censoring-mark builders for the TTE grammar}
\usage{
er_style_tte_censor_ticks(
  data,
  config,
  stratify,
  time,
  strata,
  theme,
  ...,
  shape = 3,
  size = 2,
  stroke = 0.75
)
}
\arguments{
\item{data}{The original data frame (\code{object$data}).}

\item{config}{Configuration for the censor layer (see
\code{.layer_tte_censor()}): \code{config$table} (the subset of the tidy KM
table where \code{n_censor > 0}, with a \code{strata} column when
stratified).}

\item{stratify}{Logical: whether the fit is stratified
(\code{!is.null(object$strata)}).}

\item{time}{\code{object$time} (\code{name}/\code{label}/\code{limits}).}

\item{strata}{\code{object$strata} (\code{var}/\code{label}/\code{type}/\code{n_strata}), or
\code{NULL} when unstratified.}

\item{theme}{\code{object$theme}.}

\item{...}{Additional named arguments forwarded from
\code{\link[=er_tte_add_censor]{er_tte_add_censor()}}'s own \code{...}.}

\item{shape}{Point shape for a censoring mark. Default \code{3} (a plus
sign), the conventional Kaplan-Meier censoring glyph.}

\item{size}{Point size. Default \code{2}.}

\item{stroke}{Point stroke width. Default \code{0.75}.}
}
\value{
A geom, or a list of geoms.
}
\description{
Builder functions for the \code{censor} layer (\code{\link[=er_tte_add_censor]{er_tte_add_censor()}}),
marking each censoring time directly on the Kaplan-Meier curve.
Shares the same \verb{function(data, config, stratify, time, strata, theme, ...)} signature every TTE-grammar builder implements.
}
\details{
A censoring-only row of the KM table (\code{n_censor > 0}, \code{n_event == 0})
carries the survival value the curve already had going into that
time -- Kaplan-Meier survival only drops at an \emph{event} time -- so a
mark drawn at \verb{(time, surv)} lands exactly on the step curve without
any extra lookup.

Stratified colour maps to \code{config$table}'s own \code{strata} column, the
same already-cleaned stratum label \code{\link[=er_style_tte_curve_km]{er_style_tte_curve_km()}} uses,
so a censoring mark takes on the colour of the curve it sits on. The
marks never contribute their own legend entry (\code{show.legend = FALSE}) -- the curve layer's legend already identifies each stratum.

\code{er_style_tte_censor_ticks()} is tagged \code{er_style_tag(fn, layer = "censor")}, so \code{\link[=er_tte_add_censor]{er_tte_add_censor()}} errors informatively if handed
a builder tagged for a different layer.
}
\examples{
library(survival)
lung |>
  er_tte(time, status == 2) |>
  er_tte_add_curve() |>
  er_tte_add_censor(style = er_style_tte_censor_ticks, shape = 124, size = 3) |>
  plot()

}
\seealso{
\code{\link[=er_tte_add_censor]{er_tte_add_censor()}}
}
