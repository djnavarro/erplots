% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-partials-group.R
\name{er_builder_group}
\alias{er_builder_group}
\alias{er_builder_group_boxplot}
\alias{er_builder_group_histogram}
\alias{er_builder_group_violin}
\title{Group panel builders for exposure-response plots}
\usage{
er_builder_group_boxplot(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  style
)

er_builder_group_histogram(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  style
)

er_builder_group_violin(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  style
)
}
\arguments{
\item{data}{The original data frame}

\item{config}{Configuration for the specific plot}

\item{stratify}{Logical indicating whether to stratify}

\item{exposure}{Exposure variable}

\item{response}{Response variable}

\item{strata}{Stratification variable}

\item{style}{Style components}
}
\value{
A geom, or a list of geoms; see \code{\link[=er_partial]{er_partial()}}.
}
\description{
Group panel builders for exposure-response plots
}
\details{
Builders for the \code{group} layer (\code{\link[=er_plot_add_groups]{er_plot_add_groups()}}),
which draws the exposure distribution for a grouping variable (e.g.
treatment arm) below the main panel: \code{er_builder_group_boxplot()} (the
default), \code{er_builder_group_violin()}, and \code{er_builder_group_histogram()}.
The first two put the group levels on the y-axis; \code{er_builder_group_histogram()}
instead puts them on facet strips and frees the y-axis for counts (see
\code{Details} in the package's \code{AGENTS.md}/\code{PLAN.md} for the rationale).
All three are tagged \code{er_builder_tag(fn, layer = "group")}, so
\code{\link[=er_plot_add_groups]{er_plot_add_groups()}} errors informatively if handed a builder
tagged for a different layer.

See \code{\link[=er_partial]{er_partial()}} for the shared builder interface these functions
implement, including how to write a custom builder of your own.
}
\seealso{
\code{\link[=er_partial]{er_partial()}}
}
