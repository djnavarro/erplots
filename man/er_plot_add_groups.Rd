% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-add.R
\name{er_plot_add_groups}
\alias{er_plot_add_groups}
\title{Add a grouped exposure-distribution panel}
\usage{
er_plot_add_groups(
  object,
  group_by,
  style = NULL,
  bins = NULL,
  keep_strata = NULL,
  ties = "upward",
  quantile_type = 7,
  labeller = NULL,
  ...
)
}
\arguments{
\item{object}{Partially constructed plot (has S3 class \code{er_plot}).}

\item{group_by}{Grouping variables to define groups for distribution
plots (a tidyselection of variables).}

\item{style}{Function drawing each group panel -- defaults to
\code{\link[=er_style_group_boxplot]{er_style_group_boxplot()}}. Applied to every grouping variable added
by this call; see \code{\link[=er_style]{er_style()}} and "Details".}

\item{bins}{Number of quantile bins used for continuous grouping
variables (\code{NULL}, the default, uses \code{\link[=cut_quantile]{cut_quantile()}}'s own default).
Applied identically to every grouping variable added by this call.}

\item{keep_strata}{Logical, indicating whether this layer should be
split by the plot's stratification variable; defaults to \code{TRUE} if
\code{stratify_by} was set in \code{\link[=er_plot]{er_plot()}}, \code{FALSE} otherwise. See
"Details" for an error case.}

\item{ties, quantile_type, labeller}{Passed straight through to
\code{\link[=cut_quantile]{cut_quantile()}}/\code{\link[=cut_exposure_quantile]{cut_exposure_quantile()}} to control how a
continuous grouping variable is split into bins -- see their
documentation for what each controls. Applied identically to every
grouping variable added by this call.}

\item{...}{Additional named arguments forwarded, unchanged, to \code{style}
when it's called at build time (identically for every grouping
variable added by this call) -- see \code{\link[=er_style]{er_style()}}'s "Passing extra
arguments to a builder" section. Must be named.}
}
\value{
The input \code{object}, with a group panel added.
}
\description{
Adds a group layer: a boxplot/violin panel showing the \emph{exposure}
distribution, split by one or more grouping variables (continuous
grouping variables are binned into quantiles first).
}
\details{
Unlike the other four layers, the groups layer is \strong{additive}: each call
adds another panel alongside any already added by a previous call,
rather than replacing it.

\code{\link[=er_style_group_violin]{er_style_group_violin()}} and \code{\link[=er_style_group_histogram]{er_style_group_histogram()}} are the
other built-in \code{style} options; any function matching the standard
\verb{(data, config, stratify, exposure, response, strata, theme, ...)}
signature can be supplied instead. If \code{style} is tagged with a
\code{layer} (via \code{\link[=er_style_tag]{er_style_tag()}}) other than \code{"group"}, this errors
informatively; an untagged builder is never checked.

\code{keep_strata = TRUE} errors if \code{group_by} is itself the plot's
stratification variable, since that would mean grouping and
stratifying by the same column at once; pass \code{keep_strata = FALSE}
for that grouping variable instead.

\code{bins}/\code{ties}/\code{quantile_type}/\code{labeller} are local to this call --
different grouping variables (including across separate
\code{er_plot_add_groups()} calls) aren't required to agree, and generally
shouldn't: they're usually different variables with no reason to share
a binning scheme. The one exception is grouping by the plot's own
exposure variable, which risks silently disagreeing with
\code{\link[=er_plot_add_quantiles]{er_plot_add_quantiles()}}'s own exposure-binning; \code{\link[=er_plot_build]{er_plot_build()}}
warns (doesn't error) if the two disagree in that specific case.
}
\examples{
if (requireNamespace("erglm", quietly = TRUE)) {
library(erglm)
mod <- erglm_model(ae1 ~ aucss, erglm_data, family = binomial())
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod) |>
  er_plot_add_groups(aucss) |>
  plot()

# additive: a second call adds a second panel rather than replacing the first
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod) |>
  er_plot_add_groups(aucss) |>
  er_plot_add_groups(treatment) |>
  plot()
}

}
\seealso{
\code{\link[=er_plot]{er_plot()}}, \code{\link[=er_plot_add_model]{er_plot_add_model()}}, \code{\link[=er_plot_add_summary]{er_plot_add_summary()}},
\code{\link[=er_plot_add_quantiles]{er_plot_add_quantiles()}}, \code{\link[=er_plot_add_data]{er_plot_add_data()}}, \code{\link[=er_style]{er_style()}}
}
