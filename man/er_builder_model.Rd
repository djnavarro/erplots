% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-builder-model.R
\name{er_builder_model}
\alias{er_builder_model}
\alias{er_builder_model_ribbonline}
\alias{er_builder_model_line}
\alias{er_builder_model_spaghetti}
\title{Model curve builders for exposure-response plots}
\usage{
er_builder_model_ribbonline(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  style
)

er_builder_model_line(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  style
)

er_builder_model_spaghetti(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  style
)
}
\arguments{
\item{data}{The original data frame}

\item{config}{Configuration for the specific plot}

\item{stratify}{Logical indicating whether to stratify}

\item{exposure}{Exposure variable}

\item{response}{Response variable}

\item{strata}{Stratification variable}

\item{style}{Style components}
}
\value{
A geom, or a list of geoms; see \code{\link[=er_builder]{er_builder()}}.
}
\description{
Model curve builders for exposure-response plots
}
\details{
Builders for the \code{model} layer (\code{\link[=er_plot_add_model]{er_plot_add_model()}}), which
draws the fitted curve (and, where applicable, its uncertainty) over
the exposure range: \code{er_builder_model_ribbonline()} (ribbon plus line, the
default), \code{er_builder_model_line()} (line only, no ribbon), and
\code{er_builder_model_spaghetti()} (a spaghetti plot of simulated draws, for
models that implement \code{\link[=er_simulate]{er_simulate()}}). All three are tagged
\code{er_builder_tag(fn, layer = "model")}, so \code{\link[=er_plot_add_model]{er_plot_add_model()}}
errors informatively if handed one of these swapped into the
\code{summary_builder} argument, or a builder tagged for a different layer
entirely.

See \code{\link[=er_builder]{er_builder()}} for the shared builder interface these functions
implement, including how to write a custom builder of your own.
}
\seealso{
\code{\link[=er_builder]{er_builder()}}
}
