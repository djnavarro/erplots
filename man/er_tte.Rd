% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-tte-api.R
\name{er_tte}
\alias{er_tte}
\title{The time-to-event plotting mini-language}
\usage{
er_tte(data, time, event, conf_level = 0.95)
}
\arguments{
\item{data}{Data frame or tibble containing the observed data.}

\item{time}{Event/censoring time (unquoted expression, evaluated in
\code{data}). Must be non-negative.}

\item{event}{Event indicator (unquoted expression, evaluated in
\code{data}): \code{TRUE}/\code{1} for an event, \code{FALSE}/\code{0} for censoring.}

\item{conf_level}{Confidence level for the Kaplan-Meier confidence
band. Must be strictly between 0 and 1.}
}
\value{
An (empty of layers) plot object of class \code{er_tte}, with the
single-arm Kaplan-Meier fit already computed on \code{object$km}.
}
\description{
Create an \code{er_tte} specification for a Kaplan-Meier/survival-over-time
figure. This is a separate mini-grammar from \code{\link[=er_plot]{er_plot()}}/\code{\link[=er_vpc]{er_vpc()}}:
those two share an exposure-response-vs-exposure coordinate system
(\code{er_predict()}'s contract is "response value at a given exposure"),
whereas \code{er_tte()} uses a time x-axis/survival-probability y-axis --
the natural coordinate system for a Kaplan-Meier curve, not something
\code{er_plot()}'s layers can express.
}
\details{
\code{er_tte()} computes the (single-arm) Kaplan-Meier estimate once, via
\code{survival::survfit()}, and stores the fit plus a tidy per-event-time
table (\code{time}, \code{n_risk}, \code{n_event}, \code{n_censor}, \code{surv}, \code{lower},
\code{upper}) on \code{object$km}. Layers added afterwards (curve, censoring
marks, number-at-risk table, log-rank annotation, model overlay --
none of which exist yet at this stage of development) will read from
this shared fit rather than recomputing it.

Unlike \code{\link[=er_plot]{er_plot()}}/\code{\link[=er_vpc]{er_vpc()}}, \code{time}/\code{event} accept arbitrary
tidy-eval expressions, not just bare column names -- time-to-event
data very commonly needs an inline transform to get an event
indicator (e.g. \code{status == 2} for a coded status variable, or
\code{!is.na(progression_date)}), and requiring the caller to first
\code{dplyr::mutate()} that column into existence would just be
boilerplate. The evaluated \code{time}/\code{event} vectors are stored as
\code{.er_tte_time}/\code{.er_tte_event} columns on \code{object$data}; their
\code{rlang::as_label()}-derived text is kept as \code{object$time$label}/
\code{object$event$label} for display purposes.

\code{event} must evaluate to a logical vector (\code{TRUE} = event occurred)
or a numeric vector taking only the values \code{0} (censored) and \code{1}
(event) -- exactly the same binary encoding \code{\link[=er_plot]{er_plot()}} requires of a
\code{response_type = "binary"} response.

Stratification (splitting the Kaplan-Meier estimate by exposure
quantile or another grouping variable) is not yet implemented -- this
is a single-arm-only scaffold. It will be added as a \code{stratify_by}
argument in a later change, mirroring \code{\link[=er_vpc]{er_vpc()}}'s.
}
\examples{
library(survival)
lung |>
  er_tte(time, status == 2)

}
\seealso{
\link{er_model_interface}
}
