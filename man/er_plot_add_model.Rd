% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-add.R
\name{er_plot_add_model}
\alias{er_plot_add_model}
\title{Add a fitted-model curve/ribbon layer}
\usage{
er_plot_add_model(
  object,
  model,
  keep_strata = NULL,
  style = NULL,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{object}{Partially constructed plot (has S3 class \code{er_plot}).}

\item{model}{A fitted exposure-response model. Must implement \code{\link[=er_predict]{er_predict()}}.}

\item{keep_strata}{Logical; whether this layer should use stratification.}

\item{style}{Function drawing the model curve/ribbon. Defaults to \code{\link[=er_style_model_ribbonline]{er_style_model_ribbonline()}}.}

\item{conf_level}{Confidence level for the prediction ribbon.}

\item{...}{Additional named arguments forwarded unchanged to \code{style} at build time.}
}
\value{
The input \code{object}, with the model layer added.
}
\description{
Adds the model layer: a fitted exposure-response curve with an
uncertainty ribbon, or possibly a spaghetti plot of simulated draws.
}
\details{
This layer uses \code{\link[=er_predict]{er_predict()}} to compute model predictions on the response scale. \code{model} may reference covariates beyond the exposure and strata variables. erplots fills any additional covariates from the plot data with a reference value (first factor level or numeric mean) when building the prediction grid. erplots does not check that \code{model} was fit on the same exposure/response as the plot; the caller must ensure compatibility.
}
\examples{
if (requireNamespace("erglm", quietly = TRUE)) {
library(erglm)
mod <- erglm_model(ae1 ~ aucss, erglm_data, family = binomial())
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod) |>
  plot()

# a spaghetti plot instead of the default ribbon
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod, style = er_style_model_spaghetti) |>
  plot()

# plug in a fully custom model-curve builder
build_model_dashed <- function(data, config, stratify, exposure, response, strata, theme, ...) {
  ggplot2::geom_line(
    data = config$predictions,
    mapping = ggplot2::aes(x = .data[[exposure$name]], y = fit_resp),
    linetype = "dashed"
  )
}
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod, style = build_model_dashed) |>
  plot()

# a model with a covariate beyond the exposure variable still works even when 
# this layer isn't stratifying by it: `sex` is set to a reference value 
# when building the prediction grid, which may not be what the user wants
mod_sex <- erglm_model(ae1 ~ aucss + sex, erglm_data, family = binomial())
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod_sex) |>
  plot()
}

}
\seealso{
\code{\link[=er_plot]{er_plot()}}, \code{\link[=er_plot_add_summary]{er_plot_add_summary()}}, \code{\link[=er_plot_add_quantiles]{er_plot_add_quantiles()}},
\code{\link[=er_plot_add_data]{er_plot_add_data()}}, \code{\link[=er_plot_add_groups]{er_plot_add_groups()}}, \code{\link[=er_style]{er_style()}}
}
