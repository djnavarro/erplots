% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-tte-style-pvalue.R
\name{er_style_tte_pvalue}
\alias{er_style_tte_pvalue}
\alias{er_style_tte_pvalue_logrank}
\title{Log-rank test annotation builders for the TTE grammar}
\usage{
er_style_tte_pvalue_logrank(
  data,
  config,
  stratify,
  time,
  strata,
  theme,
  ...,
  inset = 0.05,
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL
)
}
\arguments{
\item{data}{The original data frame (\code{object$data}).}

\item{config}{Configuration for the pvalue layer (see
\code{.layer_tte_pvalue()}): \code{config$p_value} (the log-rank test's
p-value) and \code{config$corner_distance} (how uncrowded each panel
corner is, relative to the plotted survival curve(s) -- see
\code{\link[=er_style]{er_style()}}'s \code{?er_plot_add_summary()}-analogous corner-placement
idiom).}

\item{stratify}{Logical: always \code{TRUE} here, since
\code{\link[=er_tte_add_pvalue]{er_tte_add_pvalue()}} requires a stratified \code{er_tte} object.}

\item{time}{\code{object$time} (\code{name}/\code{label}/\code{limits}).}

\item{strata}{\code{object$strata} (\code{var}/\code{label}/\code{type}/\code{n_strata}).}

\item{theme}{\code{object$theme} -- \code{theme$format_p} formats the p-value.}

\item{...}{Additional named arguments forwarded from
\code{\link[=er_tte_add_pvalue]{er_tte_add_pvalue()}}'s own \code{...}.}

\item{inset}{Distance from the panel edge for the annotation label,
as a fraction of the panel's width/height. Default \code{0.05}.}

\item{label_size}{Label text size.}

\item{label_colour}{Label text colour.}

\item{label_fill}{Label background fill.}
}
\value{
A geom, or a list of geoms.
}
\description{
Builder functions for the \code{pvalue} layer (\code{\link[=er_tte_add_pvalue]{er_tte_add_pvalue()}}),
drawing a corner-placed text/label annotation from a log-rank test
comparing survival across \code{stratify_by}'s levels.
}
\details{
\code{er_style_tte_pvalue_logrank()} places its annotation in whichever of
the panel's 4 corners is currently furthest from the survival
curve(s), using the same \verb{(0, 1)}-rescaled corner-distance
calculation \code{\link[=er_plot_add_summary]{er_plot_add_summary()}}'s own p-value annotation uses to
avoid a plot's raw data points -- here applied to the curve's own
\verb{(time, surv)} coordinates instead, since there's no raw per-subject
scatter in this grammar for the annotation to avoid.

\code{er_style_tte_pvalue_logrank()} is tagged \code{er_style_tag(fn, layer = "pvalue")}, so \code{\link[=er_tte_add_pvalue]{er_tte_add_pvalue()}} errors informatively if handed a
builder tagged for a different layer.
}
\examples{
library(survival)
lung |>
  transform(sex = factor(sex, labels = c("Male", "Female"))) |>
  er_tte(time, status == 2, stratify_by = sex) |>
  er_tte_add_curve() |>
  er_tte_add_pvalue(style = er_style_tte_pvalue_logrank, label_fill = "white") |>
  plot()

}
\seealso{
\code{\link[=er_tte_add_pvalue]{er_tte_add_pvalue()}}
}
