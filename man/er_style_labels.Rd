% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-style-registry.R
\name{er_style_labels}
\alias{er_style_labels}
\title{List builders registered for string-based \code{style} dispatch}
\usage{
er_style_labels(layer = NULL)
}
\arguments{
\item{layer}{A single layer name (e.g. \code{"tte_summary"}), or \code{NULL} (the
default) to list every registered label across all layers.}
}
\value{
A tibble with one row per registered \verb{(layer, label)} pair and
columns \code{layer}, \code{label}, and \code{style} (the builder function itself).
}
\description{
A builder tagged via \code{er_style_tag(fn, layer = ..., label = ...)} can be
selected with a short string (e.g. \code{style = "logrank"}) instead of the
function itself, wherever the corresponding \verb{_add_*()} function supports
it. \code{er_style_labels()} lists what's currently registered, optionally
filtered to a single \code{layer}.
}
\examples{
er_style_labels("tte_summary")

}
