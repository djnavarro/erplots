% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{poisson_interval}
\alias{poisson_interval}
\title{Exact Poisson confidence interval for a count rate}
\usage{
poisson_interval(x, n, conf_level = 0.95)
}
\arguments{
\item{x}{Vector (or sum) of observed counts, e.g. all counts falling in
one exposure bin}

\item{n}{Number of units the counts were accumulated over (e.g. the
number of observations in the bin); the rate being estimated is
\code{sum(x) / n}}

\item{conf_level}{Confidence level}
}
\value{
Named numeric vector (\code{lower}, \code{upper}) for the rate \code{sum(x) / n}, with confidence level stored as an attribute. Uses the standard
exact ("Garwood") Poisson interval, derived from the chi-squared/gamma
relationship, rather than a normal approximation. If the total count
is 0, the lower bound is 0 (there's no gamma quantile at \code{shape = 0}).
}
\description{
Exact Poisson confidence interval for a count rate
}
\details{
The count-response analogue of \code{\link[=clopper_pearson_interval]{clopper_pearson_interval()}}, used by
the quantile-binned summary layer (see \code{\link[=er_plot_show_quantiles]{er_plot_show_quantiles()}})
and \code{\link[=er_vpc_plot]{er_vpc_plot()}} when \code{response_type = "count"} is explicitly
declared. Unlike \code{\link[=t_interval]{t_interval()}} (the default, opt-in-required
approximation used when a count response auto-detects or is declared
\code{"continuous"}), this interval is exact and never produces a
negative lower bound -- see \code{PLAN.md} design decision (4) for the
rationale and history.
}
\examples{
poisson_interval(3, 10)

}
