% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-tte-style-summary.R
\name{er_style_tte_summary}
\alias{er_style_tte_summary}
\alias{er_style_tte_summary_logrank}
\alias{er_style_tte_summary_n}
\alias{er_style_tte_summary_coefficients}
\alias{er_style_tte_summary_gof}
\title{Summary annotation builders for the TTE grammar}
\usage{
er_style_tte_summary_logrank(
  data,
  config,
  stratify,
  time,
  strata,
  theme,
  ...,
  inset = 0.05,
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL
)

er_style_tte_summary_n(
  data,
  config,
  stratify,
  time,
  strata,
  theme,
  inset = 0.05,
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL,
  ...
)

er_style_tte_summary_coefficients(
  data,
  config,
  stratify,
  time,
  strata,
  theme,
  inset = 0.05,
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL,
  ...
)

er_style_tte_summary_gof(
  data,
  config,
  stratify,
  time,
  strata,
  theme,
  inset = 0.05,
  fields = c("n", "aic", "bic", "r_squared"),
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL,
  ...
)
}
\arguments{
\item{data}{The original data frame (\code{object$data}).}

\item{config}{Configuration for the summary layer (populated by
\code{\link[=er_tte_add_summary]{er_tte_add_summary()}}): \code{config$logrank_p_value} (the log-rank
test's p-value, or \code{NULL} when fewer than 2 strata levels are
present in the data), \code{config$summary} (the supplied model's raw
\code{\link[=er_summary]{er_summary()}} result, or \code{NULL} when no \code{model} was supplied), and
\code{config$corner_distance} (how uncrowded each panel corner is,
relative to the plotted survival curve(s) -- see \code{\link[=er_style]{er_style()}}'s
\code{?er_plot_add_summary()}-analogous corner-placement idiom).}

\item{stratify}{Logical: whether this layer was added with
\code{keep_strata = TRUE} (the default whenever \code{stratify_by} was set in
\code{\link[=er_tte]{er_tte()}}) -- see \code{\link[=er_tte_add_summary]{er_tte_add_summary()}}.}

\item{time}{\code{object$time} (\code{name}/\code{label}/\code{limits}).}

\item{strata}{\code{object$strata} (\code{var}/\code{label}).}

\item{theme}{\code{object$theme} -- \code{theme$format_p}/\code{theme$format_number}
format the annotation's numbers.}

\item{...}{Additional named arguments forwarded from
\code{\link[=er_tte_add_summary]{er_tte_add_summary()}}'s own \code{...}.}

\item{inset}{Distance from the panel edge for the annotation label,
as a fraction of the panel's width/height. Default \code{0.05}.}

\item{label_size}{Label text size. Defaults to \code{NULL} (\code{\link[ggplot2:geom_label]{ggplot2::geom_label()}}'s own default).}

\item{label_colour}{Label text colour. Defaults to \code{NULL} (\code{\link[ggplot2:geom_label]{ggplot2::geom_label()}}'s own default).}

\item{label_fill}{Label background fill. Defaults to \code{NULL} (\code{\link[ggplot2:geom_label]{ggplot2::geom_label()}}'s own default).}

\item{fields}{Fields from \code{glance} to include for
\code{er_style_tte_summary_gof()}, and the order they're shown in: one
or more of \code{"n"} (labelled "N"), \code{"aic"} ("AIC"), \code{"bic"} ("BIC"),
or \code{"r_squared"} (labelled "R-squared"). Defaults to all four, in
that order. A field is shown only when both present and non-\code{NA}
in the model's \code{glance} result.}
}
\value{
A geom, or a list of geoms.
}
\description{
Builder functions for the \code{summary} layer (\code{\link[=er_tte_add_summary]{er_tte_add_summary()}}),
drawing a corner-placed text/label annotation from a log-rank test
comparing survival across \code{stratify_by}'s levels, a supplied model's
\code{\link[=er_summary]{er_summary()}} result, or observation/event counts.
}
\details{
\code{er_style_tte_summary_logrank()} (the default) places its annotation
in whichever of the panel's 4 corners is currently furthest from the
survival curve(s), using the same \verb{(0, 1)}-rescaled corner-distance
calculation \code{\link[=er_plot_add_summary]{er_plot_add_summary()}}'s own p-value annotation uses to
avoid a plot's raw data points -- here applied to the curve's own
\verb{(time, surv)} coordinates instead, since there's no raw per-subject
scatter in this grammar for the annotation to avoid. It draws nothing
if \code{config$logrank_p_value} is \code{NULL} (fewer than 2 strata levels
present, including an unstratified object).

\code{er_style_tte_summary_n()} draws subject and event counts -- one line
per stratum when \code{stratify} is \code{TRUE}, a single overall line
otherwise -- and doesn't depend on a model or \code{stratify_by} at all.

\code{er_style_tte_summary_coefficients()} draws one line per row of the
supplied model's \code{coefficients} table (see \code{\link[=er_summary]{er_summary()}}'s
\code{coefficients} field); it draws nothing if \code{coefficients} wasn't
supplied, or if the layer is stratified. \code{er_style_tte_summary_gof()}
draws a single-line, comma-separated goodness-of-fit annotation from
the model's \code{glance} field -- a curated subset (\code{N}, \code{AIC}, \code{BIC},
R-squared) rather than every reserved \code{glance} column, showing only
whichever of those four are actually present and non-\code{NA}; it draws
nothing if none of them are available, or if the layer is stratified.

All four builders are tagged \code{er_style_tag(fn, layer = "tte_summary")} -- distinct from \code{\link[=er_plot_add_summary]{er_plot_add_summary()}}'s own
\code{"summary"} tag, since the two grammars' summary builders share no
signature (\code{exposure}/\code{response} vs. \code{time}) -- so
\code{\link[=er_tte_add_summary]{er_tte_add_summary()}} errors informatively if a builder tagged for a
different layer is passed to it instead (including
\code{\link[=er_plot_add_summary]{er_plot_add_summary()}}'s own builders).
}
\examples{
library(survival)
lung |>
  transform(sex = factor(sex, labels = c("Male", "Female"))) |>
  er_tte(time, status == 2, stratify_by = sex) |>
  er_tte_add_curve() |>
  er_tte_add_summary(style = er_style_tte_summary_logrank, label_fill = "white") |>
  plot()

# a purely descriptive annotation, with no log-rank test at all
lung |>
  er_tte(time, status == 2) |>
  er_tte_add_curve() |>
  er_tte_add_summary(style = er_style_tte_summary_n) |>
  plot()

}
\seealso{
\code{\link[=er_tte_add_summary]{er_tte_add_summary()}}
}
