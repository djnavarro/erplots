% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-api.R
\name{er_builder_role}
\alias{er_builder_role}
\alias{er_builder_fill_role}
\alias{er_builder_y_role}
\title{Declare a builder's fill/y-axis role}
\usage{
er_builder_fill_role(builder, role)

er_builder_y_role(builder, role)
}
\arguments{
\item{builder}{A function matching the standard \verb{er_builder_*()}
signature (see \code{\link[=er_partial]{er_partial()}})}

\item{role}{For \code{er_builder_fill_role()}: currently only \code{"density"} is
read by \code{.polish_labels()}, but any string is accepted. For
\code{er_builder_y_role()}: currently only \code{"count"} is read.}
}
\value{
\code{builder}, with an \code{"er_builder_fill_role"}/\code{"er_builder_y_role"}
attribute attached
}
\description{
Companion setters to \code{\link[=er_builder_layout]{er_builder_layout()}} for the two other pieces of
builder self-declared metadata in the package: \code{er_builder_fill_role()}
tags a data-layer builder whose \code{fill} aesthetic means something other
than strata (currently only \code{"density"}, used by \code{\link[=er_builder_data_hex]{er_builder_data_hex()}}),
and \code{er_builder_y_role()} tags a group-layer builder whose y-axis means
something other than the group variable itself (currently only
\code{"count"}, used by \code{\link[=er_builder_group_histogram]{er_builder_group_histogram()}}). Both follow the same
wrapper-function pattern as \code{\link[=er_builder_layout]{er_builder_layout()}} rather than requiring
a custom builder author to call \code{attr()} directly with a hand-typed
string constant.
}
\seealso{
\code{\link[=er_builder_layout]{er_builder_layout()}}, \code{\link[=er_partial]{er_partial()}}
}
