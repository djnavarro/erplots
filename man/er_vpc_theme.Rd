% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-vpc-theme.R
\name{er_vpc_theme}
\alias{er_vpc_theme}
\title{Adjust theme/labels for an \code{er_vpc} object}
\usage{
er_vpc_theme(
  object,
  xlab = NULL,
  ylab = NULL,
  strata_lab = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  xlim = NULL,
  ylim = NULL,
  theme_base = NULL,
  theme_extra = NULL,
  format_percent = NULL,
  format_number = NULL
)
}
\arguments{
\item{object}{Partially constructed VPC (has S3 class \code{er_vpc}).}

\item{xlab}{Label for the VPC's x-axis (single string) -- see
"Details" for why this labels \code{plot_by}, not \code{exposure}.}

\item{ylab}{Response axis label (single string).}

\item{strata_lab}{Facet strip label prefix (single string), e.g. the
\code{"Sex"} in a \code{"Sex: Female"} strip. Errors if \code{stratify_by} wasn't
set in \code{\link[=er_vpc]{er_vpc()}} -- there's no facet strip to relabel.}

\item{title, subtitle, caption}{Plot-level annotation text (single
strings).}

\item{xlim, ylim}{Axis limits (length-2, increasing numeric vectors),
applied via \code{ggplot2::coord_cartesian(clip = "off")}.}

\item{theme_base}{A ggplot2 theme object (e.g. \code{\link[ggplot2:theme_minimal]{ggplot2::theme_minimal()}})
-- the swappable overall visual theme, defaulting to
\code{\link[ggplot2:theme_bw]{ggplot2::theme_bw()}}.}

\item{theme_extra}{A ggplot2 theme object (e.g. from \code{\link[ggplot2:theme]{ggplot2::theme()}})
with additional theme tweaks layered on top of \code{theme_base}. See
"Details" for its default and replacement semantics.}

\item{format_percent, format_number}{Formatter functions (typically
from \verb{scales::label_*()}), used to format the rate/mean displayed
in the observed/simulated summaries for a binary response
(\code{format_percent}) or a continuous/count response (\code{format_number}).}
}
\value{
The input \code{object}, with the requested theme fields updated.
}
\description{
Set axis/legend labels, plot titles/captions, axis limits, theme
objects, and formatters for a VPC. This does not change which variable
is mapped to which aesthetic -- that's the builder's job via \code{style}
(see \code{\link[=er_style]{er_style()}}).
}
\details{
Every argument defaults to \code{NULL}, meaning "leave whatever was set
before unchanged". This allows repeated calls to \code{er_vpc_theme()} to
update only the supplied fields, like \code{\link[ggplot2:theme]{ggplot2::theme()}}. There is no
implicit way to reset a field to the \code{\link[=er_vpc]{er_vpc()}} default.

\code{xlab} labels \code{plot_by} (stored on \code{object$group$label}), not
\code{exposure} -- \code{plot_by} drives the VPC's actual x-axis, and the two
only coincide when the caller didn't override \code{plot_by} in \code{\link[=er_vpc]{er_vpc()}}.

\code{theme_extra} defaults to a panel border plus \code{legend.position = "bottom"}. Supplying a new value fully replaces this default rather
than merging with it, so re-include the border/legend-position
settings too if you want to keep them alongside your own additions.

Unlike \code{\link[=er_plot_theme]{er_plot_theme()}}, there is no \code{color_discrete}/\code{fill_discrete}
argument here: the observed-vs-simulated colour/fill distinction uses
a fixed, shared scale (see the "Gotchas" section of \code{AGENTS.md}) to
keep the two aligned across builders that mix colour and fill for the
same idea, and swapping it out is not yet supported. Adding \code{+ ggplot2::scale_colour_manual(...)}/\code{+ ggplot2::scale_fill_manual(...)}
to the built/returned ggplot2 object remains the escape hatch for
this, and for any other tweak not covered by this function's
arguments (e.g. \code{draw_key}, which isn't wired up for any built-in VPC
builder).
}
\examples{
if (requireNamespace("erglm", quietly = TRUE)) {
  library(erglm)
  mod <- erglm_model(ae1 ~ aucss, erglm_data, family = binomial())

  erglm_data |>
    er_vpc(aucss, ae1) |>
    er_vpc_add_observed() |>
    er_vpc_add_simulated(model = mod, seed = 1234) |>
    er_vpc_theme(
      xlab = "AUC at steady state",
      ylab = "Probability of event",
      title = "Visual predictive check"
    ) |>
    plot()
}

}
\seealso{
\code{\link[=er_vpc]{er_vpc()}}, \code{\link[=er_style]{er_style()}}
}
