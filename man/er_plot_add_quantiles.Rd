% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-api.R
\name{er_plot_add_quantiles}
\alias{er_plot_add_quantiles}
\title{Add a quantile-binned response summary layer}
\usage{
er_plot_add_quantiles(
  object,
  keep_strata = NULL,
  builder = NULL,
  bins = 4,
  conf_level = 0.95
)
}
\arguments{
\item{object}{Partially constructed plot (has S3 class \code{er_plot})}

\item{keep_strata}{Logical, indicating whether this layer should be
split by the plot's stratification variable; defaults to \code{TRUE} if
\code{stratify_by} was set in \code{\link[=er_plot]{er_plot()}}, \code{FALSE} otherwise}

\item{builder}{Function drawing the quantile summary -- defaults to
\code{\link[=er_builder_quantile_errorbar]{er_builder_quantile_errorbar()}} (point + error bar).
\code{\link[=er_builder_quantile_pointrange]{er_builder_quantile_pointrange()}} (a single \code{\link[ggplot2:geom_pointrange]{ggplot2::geom_pointrange()}})
is the other built-in option; any function matching the standard
\verb{(data, config, stratify, exposure, response, strata, style)}
signature can be supplied instead -- see \code{\link[=er_partial]{er_partial()}}.
\code{config$summary} is the pre-computed per-bin data frame (point
estimate + CI) to draw. If \code{builder} is tagged with a \code{layer} (via
\code{\link[=er_builder_tag]{er_builder_tag()}}) other than \code{"quantile"}, this errors
informatively; an untagged builder is never checked.}

\item{bins}{Number of exposure bins (not counting placebo)}

\item{conf_level}{Confidence level for the interval}
}
\value{
The input \code{object}, with the quantile layer added
}
\description{
Adds the quantile layer: exposure is cut into quantile bins (see
\code{\link[=cut_exposure_quantile]{cut_exposure_quantile()}}) and, within each bin, the response is
summarised with a point estimate and confidence interval. Which
summary/CI method is used dispatches on the plot's \code{response_type}
(set in \code{\link[=er_plot]{er_plot()}}):
\itemize{
\item \code{"binary"} -- response \emph{rate}, with a Clopper-Pearson interval (see \code{\link[=ci_clopper_pearson]{ci_clopper_pearson()}})
\item \code{"continuous"} -- bin \emph{mean}, with a t-interval (see \code{\link[=ci_t]{ci_t()}})
\item \code{"count"} -- bin \emph{mean}, with an exact Poisson interval (see \code{\link[=ci_poisson]{ci_poisson()}})
}
}
\details{
Count responses auto-detect as \code{"continuous"} (see \code{\link[=er_plot]{er_plot()}}'s
\code{response_type} parameter) and are summarised the same way as any other
continuous response unless \code{response_type = "count"} is declared
explicitly.

This layer is \strong{singleton} -- see \code{\link[=er_plot]{er_plot()}}'s "Layers are either
singleton or additive" -- so calling it twice replaces the previous
quantile summary rather than combining bins from both calls.
}
\examples{
\dontrun{
library(erglm)
mod <- erglm_model(ae1 ~ aucss, erglm_data, family = binomial())
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod) |>
  er_plot_add_quantiles() |>
  plot()

# continuous response: bin means/t-intervals instead of rates/
# Clopper-Pearson intervals, auto-detected from the response column
mod3 <- erglm_model(biomarker_change ~ aucss, erglm_data, family = gaussian())
erglm_data |>
  er_plot(aucss, biomarker_change) |>
  er_plot_add_model(mod3) |>
  er_plot_add_quantiles() |>
  plot()

# count response: declare response_type = "count" explicitly for an
# exact Poisson interval instead of the t-interval approximation used
# by the auto-detected ("continuous") default
mod4 <- erglm_model(ae_count ~ aucss, erglm_data, family = poisson())
erglm_data |>
  er_plot(aucss, ae_count, response_type = "count") |>
  er_plot_add_model(mod4) |>
  er_plot_add_quantiles() |>
  plot()

# a pointrange instead of the default errorbar
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod) |>
  er_plot_add_quantiles(builder = er_builder_quantile_pointrange) |>
  plot()

# plug in a fully custom builder; see `?er_partial` for the full contract
build_quantile_crossbar <- function(data, config, stratify, exposure, response, strata, style) {
  ggplot2::geom_crossbar(
    data = config$summary,
    mapping = ggplot2::aes(x = x_mid, y = y_mid, ymin = ci_lower, ymax = ci_upper),
    inherit.aes = FALSE
  )
}
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_add_model(mod) |>
  er_plot_add_quantiles(builder = build_quantile_crossbar) |>
  plot()
}

}
\seealso{
\code{\link[=er_plot]{er_plot()}}, \code{\link[=er_plot_add_model]{er_plot_add_model()}},
\code{\link[=er_plot_add_data]{er_plot_add_data()}}, \code{\link[=er_plot_add_groups]{er_plot_add_groups()}}, \code{\link[=er_vpc_plot]{er_vpc_plot()}},
\code{\link[=er_partial]{er_partial()}}
}
