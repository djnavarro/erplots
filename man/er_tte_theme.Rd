% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-tte-theme.R
\name{er_tte_theme}
\alias{er_tte_theme}
\title{Adjust theme/labels for an \code{er_tte} object}
\usage{
er_tte_theme(
  object,
  xlab = NULL,
  ylab = NULL,
  strata_lab = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  xlim = NULL,
  ylim = NULL,
  theme_base = NULL,
  theme_extra = NULL,
  format_p = NULL,
  format_percent = NULL,
  draw_key = NULL,
  height_curve = NULL,
  height_risktable = NULL
)
}
\arguments{
\item{object}{Partially constructed plot (has S3 class \code{er_tte}).}

\item{xlab}{Time axis label (single string). See "Details" for why
\code{xlim} (not this) drives layout decisions; \code{xlab} is purely
cosmetic.}

\item{ylab}{Survival-probability axis label (single string).}

\item{strata_lab}{Stratification legend label (single string).
Errors if \code{stratify_by} wasn't set in \code{\link[=er_tte]{er_tte()}} -- there's no
stratification legend to label.}

\item{title, subtitle, caption}{Plot-level annotation text (single
strings).}

\item{xlim}{Time-axis limits (length-2, increasing numeric vector, no
\code{NA}) -- overwrites \code{object$time$limits}. See "Details" for the
call-order caveat.}

\item{ylim}{Survival-probability axis limits (length-2, increasing
numeric vector, no \code{NA}). Purely cosmetic; defaults to \code{c(0, 1)}.}

\item{theme_base}{A ggplot2 theme object (e.g. \code{\link[ggplot2:theme_minimal]{ggplot2::theme_minimal()}})
-- the swappable overall visual theme, defaulting to
\code{\link[ggplot2:theme_bw]{ggplot2::theme_bw()}}.}

\item{theme_extra}{A ggplot2 theme object (e.g. from \code{\link[ggplot2:theme]{ggplot2::theme()}})
with additional theme tweaks layered on top of \code{theme_base}. See
"Details" for its default and replacement semantics.}

\item{format_p}{Formatter function (typically from \code{scales::label_pvalue()}),
used by \code{\link[=er_tte_add_pvalue]{er_tte_add_pvalue()}}'s annotation.}

\item{format_percent}{Formatter function (typically from
\code{scales::label_percent()}), reserved for a future TTE builder that
formats a survival probability/risk-table count as a percentage.}

\item{draw_key}{A key-glyph function (e.g. \code{\link[ggplot2:draw_key_point]{ggplot2::draw_key_point()}}),
passed as the curve/model layers' \code{key_glyph} argument.}

\item{height_curve, height_risktable}{Relative panel heights (single
positive numbers), used when \code{\link[=er_tte_add_risktable]{er_tte_add_risktable()}}'s panel is
stacked below the curve panel. Supplying only one leaves the other
unchanged.}
}
\value{
The input \code{object}, with the requested theme fields updated.
}
\description{
Set axis/legend labels, plot titles/captions, axis limits, theme
objects, formatters, the legend key glyph, and relative panel heights
for a time-to-event plot. This does not change which variable is
mapped to which aesthetic -- that's the builder's job via \code{style}
(see \link{er_style_tte_curve}).
}
\details{
Every argument defaults to \code{NULL}, meaning "leave whatever was set
before unchanged". This allows repeated calls to \code{er_tte_theme()} to
update only the supplied fields, like \code{\link[ggplot2:theme]{ggplot2::theme()}}. There is no
implicit way to reset a field to the \code{\link[=er_tte]{er_tte()}} default.

\code{xlim} overwrites \code{object$time$limits} directly (the same structural
field \code{\link[=er_tte]{er_tte()}} itself sets from the data), rather than a purely
cosmetic \code{ggplot2::coord_cartesian()} zoom -- mirroring
\code{\link[=er_plot_theme]{er_plot_theme()}}'s own \code{xlim} (not \code{\link[=er_vpc_theme]{er_vpc_theme()}}'s, which is
display-only). This matters because \code{object$time$limits} also drives
non-cosmetic defaults computed at \emph{add-layer} time: \code{\link[=er_tte_add_model]{er_tte_add_model()}}'s
default \code{time_grid} and \code{\link[=er_tte_add_risktable]{er_tte_add_risktable()}}'s default \code{times}
both span it. Call \code{er_tte_theme(xlim = ...)} \emph{before} those layers
when you want the narrower/wider range reflected in their defaults;
calling it after only changes the curve panel's own x-axis display.
\code{ylim}, by contrast, is purely cosmetic (survival probability is
always modelled on \verb{[0, 1]}; this only changes what's displayed),
stored on \code{object$theme$ylim} and defaulting to \code{c(0, 1)}.

\code{theme_extra} defaults to a panel border plus \code{legend.position = "bottom"}. Supplying a new value fully replaces this default rather
than merging with it, so re-include the border/legend-position
settings too if you want to keep them alongside your own additions.

\code{title}/\code{subtitle}/\code{caption} are applied via a single
\code{ggplot2::labs()} call on the curve panel -- unlike \code{\link[=er_plot_theme]{er_plot_theme()}}'s
\code{patchwork::plot_annotation()} indirection, this is enough even when
\code{\link[=er_tte_add_risktable]{er_tte_add_risktable()}}'s panel is stacked below it via
\code{patchwork::wrap_plots()}, since the curve panel is always the
top-most one.
}
\examples{
library(survival)
lung |>
  er_tte(time, status == 2) |>
  er_tte_add_curve() |>
  er_tte_theme(
    xlab = "Days", ylab = "Survival probability",
    title = "Overall survival"
  ) |>
  plot()

}
\seealso{
\code{\link[=er_tte]{er_tte()}}, \link{er_style_tte_curve}
}
