% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-api.R
\name{er_layout}
\alias{er_layout}
\title{Declare a data-layer builder's structural layout}
\usage{
er_layout(builder, layout = c("overlay", "panel"))
}
\arguments{
\item{builder}{A function matching the standard \verb{build_*()} signature
(see \code{\link[=er_partial]{er_partial()}})}

\item{layout}{One of \code{"overlay"} or \code{"panel"} -- see \code{\link[=er_plot_show_data]{er_plot_show_data()}}
for what each structural family means}
}
\value{
\code{builder}, with an \code{"er_layout"} attribute attached
}
\description{
Tags a \verb{build_data_*()}-style function with the \emph{structural} family it
belongs to -- \code{"overlay"} (a single call merged into the main panel) or
\code{"panel"} (one-or-more panels stacked below the base plot) -- so
\code{\link[=er_plot_show_data]{er_plot_show_data()}} can route to the right internal assembly path
(\code{.part_overlay()} vs. \code{.part_data()}) just by inspecting the \code{builder}
it was given, with no separate \code{style}/\code{layout} argument needed. See
\code{\link[=er_partial]{er_partial()}}'s "Writing your own builder" section for the full
contract; both built-in data builders (\code{\link[=build_data_overlay]{build_data_overlay()}},
\code{\link[=build_data_boxjitter]{build_data_boxjitter()}}) already carry this tag.
}
\examples{
build_data_density <- er_layout(
  function(data, config, stratify, exposure, response, strata, style) {
    ggplot2::geom_density_2d(
      data = data,
      mapping = ggplot2::aes(x = .data[[exposure$name]], y = .data[[response$name]])
    )
  },
  layout = "overlay"
)

}
\seealso{
\code{\link[=er_plot_show_data]{er_plot_show_data()}}, \code{\link[=er_partial]{er_partial()}}
}
