% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-partials-model.R
\name{build_model}
\alias{build_model}
\alias{build_model_ribbonline}
\alias{build_model_line}
\alias{build_model_spaghetti}
\title{Model curve builders for exposure-response plots}
\usage{
build_model_ribbonline(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  style
)

build_model_line(data, config, stratify, exposure, response, strata, style)

build_model_spaghetti(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  style
)
}
\arguments{
\item{data}{The original data frame}

\item{config}{Configuration for the specific plot}

\item{stratify}{Logical indicating whether to stratify}

\item{exposure}{Exposure variable}

\item{response}{Response variable}

\item{strata}{Stratification variable}

\item{style}{Style components}
}
\value{
A geom, or a list of geoms; see \code{\link[=er_partial]{er_partial()}}.
}
\description{
Model curve builders for exposure-response plots
}
\details{
Builders for the \code{model} layer (\code{\link[=er_plot_show_model]{er_plot_show_model()}}), which
draws the fitted curve (and, where applicable, its uncertainty) over
the exposure range: \code{build_model_ribbonline()} (ribbon plus line, the
default), \code{build_model_line()} (line only, no ribbon), and
\code{build_model_spaghetti()} (a spaghetti plot of simulated draws, for
models that implement \code{\link[=er_simulate]{er_simulate()}}).

See \code{\link[=er_partial]{er_partial()}} for the shared builder interface these functions
implement, including how to write a custom builder of your own.
}
\seealso{
\code{\link[=er_partial]{er_partial()}}
}
