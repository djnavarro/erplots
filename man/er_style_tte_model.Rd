% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-tte-style-model.R
\name{er_style_tte_model}
\alias{er_style_tte_model}
\alias{er_style_tte_model_line}
\title{Model-curve builders for the TTE grammar}
\usage{
er_style_tte_model_line(
  data,
  config,
  stratify,
  time,
  strata,
  theme,
  ...,
  show_ci = TRUE,
  ribbon_alpha = 0.15,
  linewidth = 1
)
}
\arguments{
\item{data}{The original data frame (\code{object$data}).}

\item{config}{Configuration for the model layer (populated by
\code{\link[=er_tte_add_model]{er_tte_add_model()}}): \code{config$predictions} (the prediction tibble
from \code{\link[=er_predict_survival]{er_predict_survival()}}, with \code{time}/\code{fit_survival}/
\code{ci_lower}/\code{ci_upper} columns), \code{config$time_grid}, \code{config$conf_level}.}

\item{stratify}{Logical: whether the fit is stratified
(\code{!is.null(object$strata)}).}

\item{time}{\code{object$time} (\code{name}/\code{label}/\code{limits}).}

\item{strata}{\code{object$strata} (\code{var}/\code{label}), or
\code{NULL} when unstratified.}

\item{theme}{\code{object$theme}.}

\item{...}{Additional named arguments forwarded from
\code{\link[=er_tte_add_model]{er_tte_add_model()}}'s own \code{...}.}

\item{show_ci}{Whether to draw the confidence band. Default \code{TRUE}.}

\item{ribbon_alpha}{Transparency of the confidence band (\code{0}-\code{1}).
Default \code{0.15}.}

\item{linewidth}{Width of the curve's line. Default \code{1}.}
}
\value{
A geom, or a list of geoms.
}
\description{
Builder functions for the \code{model} layer (\code{\link[=er_tte_add_model]{er_tte_add_model()}}),
drawing a fitted parametric \code{S(t)} curve with an optional uncertainty
band.
}
\details{
See \code{\link[=er_style_tte]{er_style_tte()}} for the shared interface every TTE-grammar
builder implements.

Unlike \code{\link[=er_style_tte_curve_km]{er_style_tte_curve_km()}}'s Kaplan-Meier step curve,
\code{config$predictions} is a smooth prediction grid (one row per
\code{newdata} row x \code{config$time_grid} value), so \code{er_style_tte_model_line()}
draws an ordinary \code{\link[ggplot2:geom_line]{ggplot2::geom_line()}}/\code{\link[ggplot2:geom_ribbon]{ggplot2::geom_ribbon()}} pair
rather than a step function.

Stratified colour/fill both map to \code{config$predictions}'s own strata
column (named after \code{strata$var}) rather than a fixed name -- unlike
\code{\link[=er_style_tte_curve_km]{er_style_tte_curve_km()}}, which always reads a column literally
named \code{strata} (the tidied Kaplan-Meier table's own naming).
\code{\link[=er_tte_build]{er_tte_build()}} still retitles the resulting legend with
\code{strata$label} afterwards.

\code{er_style_tte_model_line()} is tagged \code{er_style_tag(fn, layer = "tte_model")} -- distinct from \code{\link[=er_plot_add_model]{er_plot_add_model()}}'s own \code{"model"}
tag, since the two grammars' model builders share no signature or
\code{config} contents -- so \code{\link[=er_tte_add_model]{er_tte_add_model()}} errors informatively if
handed a builder tagged for a different layer (including
\code{\link[=er_plot_add_model]{er_plot_add_model()}}'s own builders).
}
\examples{
if (requireNamespace("ertte", quietly = TRUE)) {
  library(survival)
  library(ertte)
  mod <- ertte_aft(Surv(time, status == 2) ~ age, lung)

  lung |>
    er_tte(time, status == 2) |>
    er_tte_add_curve() |>
    er_tte_add_model(mod, style = er_style_tte_model_line, ribbon_alpha = 0.3) |>
    plot()
}

}
\seealso{
\code{\link[=er_tte_add_model]{er_tte_add_model()}}, \code{\link[=er_style_tte_curve_km]{er_style_tte_curve_km()}}, \code{\link[=er_style_tte]{er_style_tte()}}
}
