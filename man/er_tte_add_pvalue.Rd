% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-tte-add.R
\name{er_tte_add_pvalue}
\alias{er_tte_add_pvalue}
\title{Add a log-rank test annotation layer}
\usage{
er_tte_add_pvalue(object, style = NULL, ...)
}
\arguments{
\item{object}{Partially constructed plot (has S3 class \code{er_tte},
with \code{stratify_by} set -- see \code{\link[=er_tte]{er_tte()}}).}

\item{style}{Function drawing the annotation. Defaults to
\code{\link[=er_style_tte_pvalue_logrank]{er_style_tte_pvalue_logrank()}}.}

\item{...}{Additional named arguments forwarded unchanged to \code{style}
at build time (e.g. \code{\link[=er_style_tte_pvalue_logrank]{er_style_tte_pvalue_logrank()}}'s \code{inset}/
\code{label_size}/\code{label_colour}/\code{label_fill}).}
}
\value{
The input \code{object}, with the pvalue layer added.
}
\description{
Adds the pvalue layer: a corner-placed annotation of the log-rank
test comparing survival across \code{stratify_by}'s levels
(\code{survival::survdiff()}). Singleton (a second call replaces the
previous one). Requires a stratified \code{er_tte} object -- a log-rank
test compares two or more groups, so this errors if \code{stratify_by}
wasn't set in \code{\link[=er_tte]{er_tte()}}.
}
\details{
The annotation is placed in whichever corner of the panel is
currently furthest from the plotted survival curve(s), computed the
same way \code{\link[=er_plot_add_summary]{er_plot_add_summary()}}'s corner-placed annotation avoids
the raw data -- see \code{\link[=er_style_tte_pvalue_logrank]{er_style_tte_pvalue_logrank()}}.
}
\examples{
library(survival)
lung |>
  transform(sex = factor(sex, labels = c("Male", "Female"))) |>
  er_tte(time, status == 2, stratify_by = sex) |>
  er_tte_add_curve() |>
  er_tte_add_pvalue() |>
  plot()

}
\seealso{
\code{\link[=er_tte]{er_tte()}}, \code{\link[=er_style_tte_pvalue_logrank]{er_style_tte_pvalue_logrank()}}
}
