% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{ci_t}
\alias{ci_t}
\title{t-interval confidence interval for the mean of continuous data}
\usage{
ci_t(x, conf_level = 0.95)
}
\arguments{
\item{x}{Numeric vector of observations}

\item{conf_level}{Confidence level}
}
\value{
Named numeric vector (\code{lower}, \code{upper}), with confidence level
stored as an attribute. If fewer than 2 non-missing values are
supplied, returns \code{c(lower = NA, upper = NA)} (a standard deviation --
and hence a t-interval -- isn't defined for a single observation).
}
\description{
t-interval confidence interval for the mean of continuous data
}
\details{
Used by the quantile-binned summary layer (see
\code{\link[=er_plot_add_quantiles]{er_plot_add_quantiles()}}) and \code{er_vpc_plot()} to compute a
confidence interval for the mean response within an exposure bin, for
continuous (and, as an approximation, count) responses. This is the
continuous-response analogue of \code{\link[=ci_clopper_pearson]{ci_clopper_pearson()}}. \code{NA}s in \code{x} are
dropped before computing the interval.
}
\examples{
ci_t(rnorm(20))

}
