% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-style-summary.R
\name{er_style_summary}
\alias{er_style_summary}
\alias{er_style_summary_pvalue}
\alias{er_style_summary_n}
\alias{er_style_summary_coefficients}
\alias{er_style_summary_gof}
\title{Summary annotation builders for exposure-response plots}
\usage{
er_style_summary_pvalue(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  theme,
  inset = 0.05,
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL,
  ...
)

er_style_summary_n(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  theme,
  inset = 0.05,
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL,
  ...
)

er_style_summary_coefficients(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  theme,
  inset = 0.05,
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL,
  ...
)

er_style_summary_gof(
  data,
  config,
  stratify,
  exposure,
  response,
  strata,
  theme,
  inset = 0.05,
  fields = c("n", "aic", "bic", "r_squared"),
  label_size = NULL,
  label_colour = NULL,
  label_fill = NULL,
  ...
)
}
\arguments{
\item{data}{The original data frame.}

\item{config}{Configuration for the specific plot.}

\item{stratify}{Logical: whether to stratify.}

\item{exposure}{Exposure variable.}

\item{response}{Response variable.}

\item{strata}{Stratification variable.}

\item{theme}{Theme components.}

\item{inset}{Distance from the panel edge for the annotation label.}

\item{label_size}{Label text size.}

\item{label_colour}{Label text colour.}

\item{label_fill}{Label background fill.}

\item{...}{Additional named arguments forwarded from \code{\link[=er_plot_add_model]{er_plot_add_model()}}'s own \code{...}.}

\item{fields}{Fields from \code{glance} to include for \code{er_style_summary_gof()}.}
}
\value{
A geom, or a list of geoms; see \code{\link[=er_style]{er_style()}}.
}
\description{
Summary annotation builders for exposure-response plots
}
\details{
Builders for \code{\link[=er_plot_add_summary]{er_plot_add_summary()}} annotate the base panel
with a summary statistic or descriptive label. \code{er_style_summary_pvalue()}
draws a formatted p-value from the model's \code{\link[=er_summary]{er_summary()}} result;
\code{er_style_summary_n()} draws observation counts and doesn't have to
originate from a fitted model at all.
\code{er_style_summary_coefficients()} draws one line per row of the model's
\code{coefficients} table (see \code{\link[=er_summary]{er_summary()}}'s \code{coefficients} field), useful
for models with several parameters and no single privileged p-value
(e.g. a multi-parameter nonlinear model); it draws nothing if
\code{coefficients} wasn't supplied, or if the layer is stratified.
\code{er_style_summary_gof()} draws a single-line, comma-separated
goodness-of-fit annotation from the model's \code{glance} field (see
\code{\link[=er_summary]{er_summary()}}) -- a curated subset (\code{N}, \code{AIC}, \code{BIC}, \verb{R\\u00b2}) rather
than every reserved \code{glance} column, showing only whichever of those
four are actually present and non-\code{NA}; it draws nothing if none of them
are available, or if the layer is stratified. All four builders are
tagged \code{er_style_tag(fn, layer = "summary")}, so \code{\link[=er_plot_add_summary]{er_plot_add_summary()}}
errors informatively if a builder tagged for a different layer is passed
to it instead.

See \code{\link[=er_style]{er_style()}} for the shared builder interface these functions
implement, including how to write a custom builder of your own.
}
\examples{
if (requireNamespace("erglm", quietly = TRUE)) {
  library(erglm)
  mod <- erglm_model(ae1 ~ aucss, erglm_data, family = binomial())

  # er_style_summary_pvalue(): the default, drawn from the model's own
  # er_summary()
  erglm_data |>
    er_plot(aucss, ae1) |>
    er_plot_add_model(mod) |>
    er_plot_add_summary(model = mod, style = er_style_summary_pvalue) |>
    plot()

  # er_style_summary_n(): model-agnostic observation count
  erglm_data |>
    er_plot(aucss, ae1) |>
    er_plot_add_model(mod) |>
    er_plot_add_summary(style = er_style_summary_n) |>
    plot()
}

}
\seealso{
\code{\link[=er_style]{er_style()}}
}
