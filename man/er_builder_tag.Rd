% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-api.R
\name{er_builder_tag}
\alias{er_builder_tag}
\title{Tag a builder with structural/aesthetic metadata}
\usage{
er_builder_tag(builder, layout = NULL, fill_role = NULL, y_role = NULL)
}
\arguments{
\item{builder}{A function matching the standard \verb{er_builder_*()} signature
(see \code{\link[=er_partial]{er_partial()}})}

\item{layout}{One of \code{"overlay"} or \code{"panel"}, or \code{NULL} (the default) to
leave this tag unset -- see \code{\link[=er_plot_add_data]{er_plot_add_data()}} for what each
structural family means}

\item{fill_role}{A string naming what the builder's \code{fill} aesthetic
represents (currently only \code{"density"} is read by \code{.polish_labels()},
but any string is accepted), or \code{NULL} (the default) to leave this tag
unset}

\item{y_role}{A string naming what the builder's y-axis represents
(currently only \code{"count"} is read by \code{.polish_labels()}), or \code{NULL}
(the default) to leave this tag unset}
}
\value{
\code{builder}, with whichever of the \code{"er_builder_layout"}/
\code{"er_builder_fill_role"}/\code{"er_builder_y_role"} attributes were
requested attached
}
\description{
Attaches the self-declared metadata a custom \verb{er_builder_*()}-style
function can carry, in a single call: which \emph{structural} family a
data-layer builder belongs to (\code{layout}), what a builder's \code{fill}
aesthetic means when it isn't strata (\code{fill_role}), and what a
group-layer builder's y-axis means when it isn't the group variable
itself (\code{y_role}). All three arguments are optional and independent --
pass only the ones a given builder needs, in one call, rather than
chaining separate setters. See \code{\link[=er_partial]{er_partial()}}'s "Writing your own
builder" section for the full contract.
}
\details{
\code{layout} is the one required tag for a data-layer builder:
\code{\link[=er_plot_add_data]{er_plot_add_data()}} reads it off \code{builder} to decide whether to route
through \code{.part_overlay()} (\code{"overlay"}: a single call merged into the
main panel, at the observations' true \verb{(exposure, response)}
coordinates) or \code{.part_data()} (\code{"panel"}: one-or-more panels stacked
below the base plot), \emph{before} it can call the builder -- so the choice
can't be inferred from the builder's return value. Both built-in data
builders (\code{\link[=er_builder_data_overlay]{er_builder_data_overlay()}}, \code{\link[=er_builder_data_boxjitter]{er_builder_data_boxjitter()}})
already carry this tag.

\code{fill_role} and \code{y_role} are both optional, read by \code{.polish_labels()}
to title a legend/axis correctly: \code{fill_role = "density"} (used by
\code{\link[=er_builder_data_hex]{er_builder_data_hex()}}) says a builder's \code{fill} aesthetic encodes bin
density rather than strata; \code{y_role = "count"} (used by
\code{\link[=er_builder_group_histogram]{er_builder_group_histogram()}}) says a group-layer builder's y-axis
means counts rather than the group variable itself. A builder that
omits either tag keeps the default behaviour (\code{fill} means strata;
the y-axis is titled with the group variable's label), which is
correct for most builders.
}
\examples{
build_data_density <- er_builder_tag(
  function(data, config, stratify, exposure, response, strata, style) {
    ggplot2::geom_density_2d(
      data = data,
      mapping = ggplot2::aes(x = .data[[exposure$name]], y = .data[[response$name]])
    )
  },
  layout = "overlay"
)

}
\seealso{
\code{\link[=er_plot_add_data]{er_plot_add_data()}}, \code{\link[=er_partial]{er_partial()}}
}
