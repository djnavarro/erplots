% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-plot-api.R
\name{er_plot_show_groups}
\alias{er_plot_show_groups}
\title{Add a grouped exposure-distribution panel}
\usage{
er_plot_show_groups(
  object,
  group_by,
  style = "boxplot",
  bins = NULL,
  keep_strata = NULL
)
}
\arguments{
\item{object}{Partially constructed plot (has S3 class \code{er_plot})}

\item{group_by}{Grouping variables to define groups for distribution
plots (a tidyselection of variables)}

\item{style}{Character string selecting the partial builder:
\code{"boxplot"} (default) or \code{"violin"}}

\item{bins}{Number of quantile bins used for continuous grouping
variables (\code{NULL}, the default, uses \code{\link[=cut_quantile]{cut_quantile()}}'s own default)}

\item{keep_strata}{Logical, indicating whether this layer should be
split by the plot's stratification variable; defaults to \code{TRUE} if
\code{stratify_by} was set in \code{\link[=er_plot]{er_plot()}}, \code{FALSE} otherwise}
}
\value{
The input \code{object}, with a group panel added
}
\description{
Adds a group layer: a boxplot/violin panel showing the \emph{exposure}
distribution, split by one or more grouping variables (continuous
grouping variables are binned into quantiles first, via
\code{\link[=cut_quantile]{cut_quantile()}}). This layer only looks at the exposure variable, not
the response, so it needs no \code{response_type} dispatch.
}
\details{
Unlike the other three layers, this one is \strong{additive} -- see
\code{\link[=er_plot]{er_plot()}}'s "Layers are either singleton or additive" -- each call
adds another panel alongside any already added by a previous call,
rather than replacing it.
}
\examples{
\dontrun{
library(erglm)
mod <- erglm_model(ae1 ~ aucss, erglm_data, family = binomial())
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_show_model(mod) |>
  er_plot_show_groups(aucss) |>
  plot()

# additive: a second call adds a second panel rather than replacing the first
erglm_data |>
  er_plot(aucss, ae1) |>
  er_plot_show_model(mod) |>
  er_plot_show_groups(aucss) |>
  er_plot_show_groups(treatment) |>
  plot()
}

}
\seealso{
\code{\link[=er_plot]{er_plot()}}, \code{\link[=er_plot_show_model]{er_plot_show_model()}},
\code{\link[=er_plot_show_quantiles]{er_plot_show_quantiles()}}, \code{\link[=er_plot_show_datastrip]{er_plot_show_datastrip()}}
}
