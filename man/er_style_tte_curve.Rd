% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/er-tte-style-curve.R
\name{er_style_tte_curve}
\alias{er_style_tte_curve}
\alias{er_style_tte_curve_km}
\title{Kaplan-Meier curve builders for the TTE grammar}
\usage{
er_style_tte_curve_km(
  data,
  config,
  stratify,
  time,
  strata,
  theme,
  ...,
  show_ci = TRUE,
  ribbon_alpha = 0.15,
  linewidth = 1
)
}
\arguments{
\item{data}{The original data frame (\code{object$data}).}

\item{config}{Configuration for the curve layer (see
\code{.layer_tte_curve()}): \code{config$table} (the tidy KM table, with a
\verb{(0, 1)} origin row prepended per stratum), \code{config$time_upper}
(the time-axis upper limit, needed so the last confidence-band
interval has somewhere to end), \code{config$conf_level}.}

\item{stratify}{Logical: whether the fit is stratified
(\code{!is.null(object$strata)}).}

\item{time}{\code{object$time} (\code{name}/\code{label}/\code{limits}).}

\item{strata}{\code{object$strata} (\code{var}/\code{label}/\code{type}/\code{n_strata}), or
\code{NULL} when unstratified.}

\item{theme}{\code{object$theme}.}

\item{...}{Additional named arguments forwarded from
\code{\link[=er_tte_add_curve]{er_tte_add_curve()}}'s own \code{...}.}

\item{show_ci}{Whether to draw the confidence band. Default \code{TRUE}.}

\item{ribbon_alpha}{Transparency of the confidence band (\code{0}-\code{1}).
Default \code{0.15}.}

\item{linewidth}{Width of the step curve's line. Default \code{1}.}
}
\value{
A geom, or a list of geoms.
}
\description{
Builder functions for the \code{curve} layer (\code{\link[=er_tte_add_curve]{er_tte_add_curve()}}),
drawing the Kaplan-Meier estimate as a step function with an optional
step-shaped confidence band. Shares the same \verb{function(data, config, stratify, time, strata, theme, ...)} signature every TTE-grammar
builder implements -- the TTE analogue of \code{\link[=er_style]{er_style()}}'s shared
interface for the \code{er_plot()} grammar, adapted for a time x-axis/
survival-probability y-axis instead of exposure/response.
}
\details{
A Kaplan-Meier confidence band is a step function, just like the
curve itself, but ggplot2 has no built-in "step ribbon" geom (unlike
\code{\link[ggplot2:geom_step]{ggplot2::geom_step()}} for the line). \code{er_style_tte_curve_km()} works
around this with \code{\link[ggplot2:geom_rect]{ggplot2::geom_rect()}}: one rectangle per interval
between consecutive event/censoring times, with \code{xmin}/\code{xmax} the
interval's start/end time and \code{ymin}/\code{ymax} the interval's constant
\code{lower}/\code{upper} bound -- visually identical to a step ribbon, without
needing a bespoke stat.

Stratified colour/fill both map to \code{config$table}'s own \code{strata}
column (the already-cleaned stratum label, e.g. \code{"Q1"} or a
categorical level) rather than the original \code{stratify_by} column on
\code{data}, since that's what \code{config$table} actually carries -- there is
no polishing step yet (analogous to \code{er_plot()}'s \code{.polish_labels()})
to retitle the resulting legend with \code{strata$label} instead of the
literal \code{"strata"}.

\code{er_style_tte_curve_km()} is tagged \code{er_style_tag(fn, layer = "curve")}, so \code{\link[=er_tte_add_curve]{er_tte_add_curve()}} errors informatively if handed a
builder tagged for a different layer.
}
\examples{
library(survival)
lung |>
  er_tte(time, status == 2) |>
  er_tte_add_curve(style = er_style_tte_curve_km, ribbon_alpha = 0.3) |>
  plot()

}
\seealso{
\code{\link[=er_tte_add_curve]{er_tte_add_curve()}}
}
