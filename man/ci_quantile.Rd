% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{ci_quantile}
\alias{ci_quantile}
\title{Distribution-free confidence interval for a sample quantile}
\usage{
ci_quantile(x, prob = 0.5, conf_level = 0.95)
}
\arguments{
\item{x}{Numeric vector of observations}

\item{prob}{Quantile probability (e.g. \code{0.1} for the 10th percentile)}

\item{conf_level}{Confidence level}
}
\value{
Named numeric vector (\code{lower}, \code{upper}), with confidence level
stored as an attribute. Returns \code{c(lower = NA, upper = NA)} if fewer
than 2 non-missing values are supplied.
}
\description{
Computes a nonparametric confidence interval for a sample quantile using
the order-statistic method (Conover, \emph{Practical Nonparametric
Statistics}): the interval endpoints are order statistics of \code{x}, chosen
via the binomial distribution of ranks so that no assumption is made
about the shape of \code{x}'s distribution.
}
\details{
Used by \code{\link[=er_vpc_add_observed]{er_vpc_add_observed()}} to compute a confidence interval
for each requested percentile of the observed response within an
exposure bin (the observed-side analogue of the across-replicate
percentile interval \code{\link[=er_vpc_add_simulated]{er_vpc_add_simulated()}} gets from simulated data,
powering \code{\link[=er_style_vpc_observed_pointrange_continuous]{er_style_vpc_observed_pointrange_continuous()}}). Like
\code{\link[=ci_clopper_pearson]{ci_clopper_pearson()}}, this interval is exact for its target coverage
but conservative -- the discreteness of the binomial rank distribution
means the achieved coverage can exceed the nominal \code{conf_level},
especially for a small bin or an extreme \code{prob}. The candidate rank
indices are clipped to \verb{[1, length(x)]}, so a very small or extreme-\code{prob}
bin returns a (still valid, but wider-than-nominal) interval built from
the most extreme order statistics available rather than \code{NA}.
}
\examples{
ci_quantile(rnorm(100), prob = 0.1)

}
